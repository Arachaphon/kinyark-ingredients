= Schema Alignment + Registration Implementation Report
:toc: left
:toclevels: 3

== Summary

This branch merges two feature sets:

1. **Schema Alignment** (`chore/schema-alignment`) — Renamed all validation schema keys and API JSON body keys from snake_case to camelCase to match Prisma conventions. Added `Recipe.visibility` and `RecipeEquipment` model. Removed unused `Ingredient.role` and `Ingredient.discount` fields.

2. **Registration** (`feature/register-user`) — End-to-end user registration with password strength validation, duplicate email/username checks, role assignment, and comprehensive test coverage.

Branch: `feature/register-user` (schema-alignment merged in)

== Test Results

[source]
----
Test Suites: 4 passed, 4 total
Tests:       19 todo, 40 passed, 59 total
Time:        1.798 s
----

== Files Modified (combined)

[source]
----
prisma/schema.prisma
src/lib/validations/auth.schema.ts          (enhanced: password strength + role + login by username)
src/lib/validations/recipe.schema.ts        (snake_case → camelCase)
src/lib/validations/review.schema.ts        (snake_case → camelCase)
src/lib/validations/ingredient.schema.ts    (snake_case → camelCase)
src/lib/validations/search.schema.ts        (snake_case → camelCase)
src/app/api/recipes/route.ts                (updated field references)
src/app/api/reviews/route.ts                (updated field references)
src/app/api/reviews/[id]/route.ts           (updated field references)
src/app/api/favorites/route.ts              (recipe_id → recipeId)
src/app/(main)/create-recipe/action.ts      (updated field references)
src/app/(auth)/register/actions.ts          (rebuilt: Prisma + duplicate check + role)
src/app/(auth)/register/page.tsx            (unchanged UI, role passed via formData)
src/app/(auth)/login/actions.tsx            (login by username/email)
tests/register.test.ts                      (NEW: 13 unit tests)
tests/register.api.test.ts                  (NEW: 14 integration tests)
tests/validation.test.ts                    (camelCase test data + review base fix)
----

See detailed reports under each section for full breakdown.