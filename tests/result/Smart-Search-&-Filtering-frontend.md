# 🧪 Manual Test Cases: Smart Search & Filtering

**Document Version:** 1.0
**Module:** Smart Search & Filtering (Frontend)
**Components:** Navbar Search, Ingredient Selection Page, Search Results Page (รวม Debounce, Skeleton, Empty State และ AI Mockup)

---

## 📋 ตารางทดสอบ (Test Cases)

### Scenario 1: การค้นหาผ่าน Navbar (Global Search)

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-01** | 1. ไปที่หน้าเว็บไซต์ที่มี Navbar<br>2. คลิกที่ช่องค้นหา "ค้นหา..."<br>3. สังเกตการแสดงผล | ช่องค้นหาสามารถคลิกได้ และพร้อมรับการพิมพ์ข้อความ | |
| **TC-02** | 1. พิมพ์คำว่า "สลัด" ลงในช่องค้นหา<br>2. สังเกต Dropdown ที่แสดงขึ้นมา | ระบบแสดงกล่อง Dropdown และแนะนำคำที่เกี่ยวข้องกับ "สลัด" เช่น "สลัดผักสวนครัว", "สลัดผลไม้", "สลัดไก่" | |
| **TC-03** | 1. พิมพ์คำที่ไม่มีในระบบ เช่น "พิซซ่า"<br>2. สังเกต Dropdown | Dropdown แสดงข้อความตัวเอียงว่า *"ไม่พบสูตรอาหารสำหรับ 'พิซซ่า'"* | |
| **TC-04** | 1. พิมพ์คำว่า "สลัด"<br>2. กดปุ่ม `Enter` บนคีย์บอร์ด หรือ คลิกเลือกคำใน Dropdown | กล่อง Dropdown ปิดลง และระบบนำทาง (Redirect) ไปที่หน้า `/search/results?query=สลัด` | |

---

### Scenario 2: การค้นหาผ่านหน้าเลือกวัตถุดิบ (Ingredient Selection)

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-05** | 1. เข้าไปที่หน้า `/search`<br>2. คลิกเปลี่ยนหมวดหมู่ด้านซ้าย (เช่น จาก "เนื้อสัตว์" เป็น "ผัก") | หมวดหมู่ที่ถูกเลือกจะมีไฮไลท์สีเขียวขอบซ้าย และรายการวัตถุดิบด้านขวาเปลี่ยนเป็นผัก | |
| **TC-06** | 1. พิมพ์ค้นหาในช่อง "ค้นหาในผัก..." (เช่น พิมพ์ "มะ") | รายการผักด้านล่างถูกกรองให้เหลือแค่คำที่มี "มะ" (เช่น มะเขือเทศ, มะนาว) ทันที | |
| **TC-07** | 1. ติ๊ก Checkbox เลือก "มะเขือเทศ" และ "หัวหอม"<br>2. สังเกตแถบ "ที่เลือก:" ด้านล่าง | Checkbox เปลี่ยนเป็นสีดำพร้อมเครื่องหมายถูก และด้านล่างมีป้ายกำกับ (Tag) "มะเขือเทศ", "หัวหอม" ปรากฏขึ้น | |
| **TC-08** | 1. กดปุ่ม "ค้นหาสูตรอาหาร ➔" | ระบบนำทางไปที่ `/search/results?category=Vegetables&ingredients=มะเขือเทศ,หัวหอม` | |

---

### Scenario 3: การแสดงผลหน้าผลลัพธ์ (Search Results Page)

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-09** | 1. เข้าหน้าผลลัพธ์จากการค้นหา (เช่น จาก TC-04 หรือ TC-08)<br>2. สังเกตหน้าจอในช่วง 1.5 วินาทีแรก | ระบบแสดงข้อความ "กำลังค้นหา..." และแสดงโครงสร้างการ์ดสีเทากระพริบ (Loading Skeleton) 4 ใบ | |
| **TC-10** | 1. รอจนกว่าระบบจะโหลดข้อมูลเสร็จ (หลัง 1.5 วินาที)<br>2. ตรวจสอบการเรียงลำดับของการ์ด | การ์ดใบที่ 1: เป็นของ AI Gemini (ขอบสีฟ้า, มีป้าย AI Generated)<br>การ์ดใบที่ 2: เป็นของ AI DeepSeek (ขอบสีดำ, มีป้าย AI Generated)<br>การ์ดที่ 3 เป็นต้นไป: เป็นของ User ทั่วไป (ขอบเขียว) เรียงตามคะแนน Rating | |
| **TC-11** | 1. ตรวจสอบข้อมูลภายในของการ์ด 1 ใบ | ข้อมูลแสดงครบถ้วน: รูปภาพ, ชื่อสูตร, ป้ายวัตถุดิบ (Tags), รูปโปรไฟล์ผู้แต่ง, ชื่อผู้แต่ง, ยอดหัวใจ, และคะแนนดาว | |

---

### Scenario 4: ระบบ Smart Search (Debounce) ในหน้าผลลัพธ์

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-12** | 1. ในหน้าผลลัพธ์ พิมพ์คำค้นหาใหม่ในช่อง Smart Search (เช่น "ต้มยำกุ้ง") แบบพิมพ์ต่อเนื่องรวดเร็ว<br>2. สังเกตการทำงานของระบบระหว่างที่มือยังพิมพ์อยู่ | ระบบยังไม่ทำการค้นหา และไม่มี Skeleton ขึ้นมารบกวนการพิมพ์ (ระบบ Debounce หน่วงเวลาทำงานถูกต้อง) | |
| **TC-13** | 1. หยุดพิมพ์และปล่อยมือประมาณ 0.5 วินาที (500ms) | ระบบเริ่มเข้าสู่สถานะ "กำลังค้นหา..." แสดง Skeleton กระพริบ และดึงข้อมูลชุดใหม่มาแสดง | |

---

### Scenario 5: การจัดการหน้าจอเมื่อค้นหาไม่พบ (Empty State)

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-14** | 1. ในช่อง Smart Search ของหน้าผลลัพธ์ ให้กดลบคำค้นหาออกจนหมด (หรือเว้นว่างไว้)<br>2. รอระบบประมวลผล | ระบบแสดงหน้าจอ Empty State ประกอบด้วยไอคอนรูปจาน 🍽️ สีเทา และข้อความ "ไม่พบสูตรอาหาร" พร้อมคำแนะนำให้ค้นหาใหม่ | |

---

### Scenario 6: การทำงานของ UI ภายในหน้าผลลัพธ์ (Interactions)

| Test Case ID | Test Steps (ขั้นตอนการทดสอบ) | Expected Results (ผลลัพธ์ที่คาดหวัง) | Status (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| **TC-15** | 1. คลิกที่ไอคอนรูปหัวใจ 🤍 ตรงมุมขวาของการ์ดสูตรอาหาร (ที่ยังไม่ได้กด) | หัวใจเปลี่ยนเป็นสีแดง (Fill Color) และตัวเลขยอดไลก์เพิ่มขึ้น 1 ทันที (เช่น จาก 22 เป็น 23) | |
| **TC-16** | 1. คลิกที่ไอคอนรูปหัวใจ ❤️ ซ้ำอีกครั้ง (กดยกเลิก) | หัวใจกลับมาเป็นสีเทา (Outline) และตัวเลขยอดไลก์ลดลงกลับไปเท่าเดิม | |
| **TC-17** | 1. นำเมาส์ไปชี้ (Hover) ที่การ์ดสูตรอาหาร หรือ ปุ่ม "ดูสูตรอาหาร" | การ์ดมีเงา (Shadow) นูนขึ้นมา และปุ่มดูสูตรอาหารมีการตอบสนอง (Hover Effect) เปลี่ยนสีเข้มขึ้น | |

---

**💡 คำแนะนำเพิ่มเติมสำหรับ QA / Tester:**
> ปัจจุบันระบบใช้ Mock Data และ `setTimeout` เพื่อจำลองการตอบกลับของ API เมื่อมีการเชื่อมต่อฝั่ง Backend (Prisma & AI Services) เรียบร้อยแล้ว ควรเพิ่ม Test Case สำหรับตรวจสอบการจัดการ Error State (เช่น API Timeout หรือ Database Connection Error) เพิ่มเติมด้วย