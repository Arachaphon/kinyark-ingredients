# 🧪 Test Cases: Home Feed & Recipe Details Features

เอกสารชุดนี้ใช้สำหรับทดสอบการทำงานของระบบหน้าบ้าน (Frontend) ในส่วนของหน้า Home Feed, หน้าแสดงรายละเอียดสูตรอาหาร (Recipe Details) และระบบกดถูกใจ (Favorite) ที่มีการทำงานร่วมกับ API และระบบ Fallback (Mock Data)

---

## 🏠 1. หน้า Home Feed & Pagination (`/home`)

| Test Case ID | หัวข้อการทดสอบ | ขั้นตอนการทดสอบ (Steps) | ผลลัพธ์ที่คาดหวัง (Expected Result) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-HOME-01** | ตรวจสอบการแสดงผลข้อมูล Hero Section (API Success) | 1. เข้าสู่หน้า `/home`<br>2. รอให้ระบบโหลดข้อมูลจาก `/api/recipes/featured` | ระบบแสดงผลสูตรอาหารแนะนำพร้อมรูปภาพหน้าปก, ชื่อเมนู และคะแนนดาวได้อย่างถูกต้อง | [ ] |
| **TC-HOME-02** | ตรวจสอบระบบ Fallback (API Error/Pending) | 1. จำลองการปิด API หรือให้ API ส่ง Error 500 กลับมา<br>2. รีเฟรชหน้า `/home` | หน้าเว็บไม่แสดงหน้าจอโหลดค้างหรือ Error แต่ดึง Mock Data (สลัด) มาแสดงผลแทนได้อย่างแนบเนียน | [ ] |
| **TC-HOME-03** | ตรวจสอบการนำทางหมวดหมู่วัตถุดิบ | 1. คลิกที่การ์ดหมวดหมู่ (เช่น "เนื้อสัตว์") ในส่วนของหมวดหมู่วัตถุดิบ | ระบบนำทางไปยัง URL `/search?category=Meat` (หรือตามหมวดหมู่ที่คลิก) | [ ] |
| **TC-HOME-04** | ตรวจสอบระบบ Pagination ของ Carousel | 1. เลื่อนลงมาที่ "สูตรอาหารแนะนำประจำสัปดาห์"<br>2. กดปุ่มลูกศร `>` เพื่อเลื่อนดูสูตรอาหารไปเรื่อยๆ จนถึงรูปก่อนสุดท้าย | เมื่อถึงรูปก่อนสุดท้าย ระบบจะแอบเรียก API หน้าถัดไป (`page=2`) แบบเงียบๆ และนำการ์ดมาต่อคิวให้สามารถกดดูต่อได้แบบไม่สะดุด | [ ] |
| **TC-HOME-05** | ตรวจสอบ Carousel Fallback (Mock Data) | 1. จำลอง API `/api/recipes?aiProvider=...` ให้ใช้งานไม่ได้<br>2. กดเลื่อน Carousel | Carousel ยังคงใช้งานได้ปกติโดยดึง Mock Data ที่เตรียมไว้มาแสดงผลแบบวนลูป | [ ] |

---

## 📖 2. หน้า Recipe Details (`/recipe/[id]`)

| Test Case ID | หัวข้อการทดสอบ | ขั้นตอนการทดสอบ (Steps) | ผลลัพธ์ที่คาดหวัง (Expected Result) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-RECIPE-01** | ตรวจสอบการโหลดข้อมูลสูตรอาหารสำเร็จ | 1. เข้าสู่หน้า `/recipe/[valid_id]` | ระบบแสดงข้อมูลครบถ้วน: รูปโปรไฟล์ผู้แต่ง, ชื่อเมนู, คะแนนดาว, รูปภาพ/วิดีโอ, ส่วนผสม และวิธีทำ | [ ] |
| **TC-RECIPE-02** | ตรวจสอบสถานะ 404 (Not Found) | 1. เข้าสู่หน้า `/recipe/[invalid_id]` ที่ไม่มีในระบบ | ระบบแสดงกล่องข้อความ "ไม่พบสูตรอาหารนี้" พร้อมปุ่มกดกลับไปหน้าก่อนหน้า | [ ] |
| **TC-RECIPE-03** | ตรวจสอบระบบ Fallback (Mock Data) ในหน้า Detail | 1. ปิด API หรือตั้งค่าให้โยน Error<br>2. เข้าสู่หน้า `/recipe/123` | ระบบดึงข้อมูล Mock Data (สปาเก็ตตี้คาโบนาร่า) มาแสดงผลให้ทดสอบ UI ได้โดยไม่แสดงหน้า Error | [ ] |
| **TC-RECIPE-04** | ตรวจสอบการทำงานของ Image Carousel | 1. เปิดสูตรอาหารที่มีรูปภาพมากกว่า 1 รูป<br>2. กดปุ่ม `<` หรือ `>` บนรูปภาพ<br>3. กดเลือกรูปย่อ (Thumbnails) ด้านล่าง | รูปภาพหลักเปลี่ยนตามการกด พร้อมไฮไลต์ขอบสีเขียวที่รูปย่อด้านล่าง และตัวเลขหน้า/จำนวนรูป อัปเดตถูกต้อง | [ ] |
| **TC-RECIPE-05** | ตรวจสอบ UI เซ็ทอาหารร้านค้า (Store Post) | 1. เปิดสูตรอาหารที่มีข้อมูล `storePosts`<br>2. ตรวจสอบ UI ส่วนบน | แสดงกล่อง "รายละเอียดเซ็ทอาหารร้านค้า" พร้อม ราคา, ช่องทางติดต่อ, รายการวัตถุดิบในเซ็ท และแผนที่ Google Maps | [ ] |
| **TC-RECIPE-06** | ตรวจสอบการเปิด/ปิด ความคิดเห็น | 1. คลิกที่ปุ่ม "ความคิดเห็น" ใต้ชื่อสูตรอาหาร | กล่องแสดงความคิดเห็นจะเปิดขยายลงมาด้านล่าง (พร้อม Animation Fade-in) และเมื่อกดซ้ำจะปิดลง | [ ] |

---

## ❤️ 3. ระบบกดถูกใจ (Favorite)

| Test Case ID | หัวข้อการทดสอบ | ขั้นตอนการทดสอบ (Steps) | ผลลัพธ์ที่คาดหวัง (Expected Result) | สถานะ |
| :--- | :--- | :--- | :--- | :---: |
| **TC-FAV-01** | ตรวจสอบการแสดงผล Animation กดถูกใจ | 1. เข้าสู่หน้า Recipe Details<br>2. คลิกที่ไอคอนรูปหัวใจ | ไอคอนหัวใจเปลี่ยนเป็นสีแดง (Fill) พร้อมมี Animation ย่อขยาย ตัวเลขจำนวนคนถูกใจเพิ่มขึ้น `+1` | [ ] |
| **TC-FAV-02** | ตรวจสอบการยกเลิกกดถูกใจ (Toggle) | 1. คลิกที่ไอคอนรูปหัวใจที่กดถูกใจไว้แล้ว (สีแดง) | ไอคอนหัวใจเปลี่ยนกลับเป็นแบบเส้นขอบ (Outline) สีเทา ตัวเลขจำนวนคนถูกใจลดลง `-1` | [ ] |
| **TC-FAV-03** | ตรวจสอบ API Integration & Fallback | 1. คลิกที่ไอคอนรูปหัวใจขณะที่เชื่อมต่อ API จริงไม่ได้ | UI ตอบสนองการกดทันที (เปลี่ยนสีและตัวเลข) เพื่อประสบการณ์การใช้งานที่ดี (Optimistic UI) ตาม Logic Fallback ที่ตั้งไว้ | [ ] |

---
**หมายเหตุสำหรับผู้ทดสอบ (QA):**
* หากต้องการทดสอบ API จริง ให้ตรวจสอบให้แน่ใจว่าได้ล้าง Mock Data หรือตั้งค่า Environment ฝั่ง Backend ให้พร้อมใช้งาน
* การทำงานของ Google Maps จะอิงจากข้อความในฟิลด์ `storeLocation`